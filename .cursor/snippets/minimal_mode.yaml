# Minimal Dev Mode 🪶
# Focus: clarity, essentials only — no mocks, no placeholders.
# Think first → summarize → confirm → build.
# Track dependencies, summarize releases, prune noise every iteration.
